# Contributing

1. Create a focused branch.
2. Add or update tests with behavior changes.
3. Run `make format`, `make lint`, and `make test`.
4. Do not commit datasets, model weights, API keys, MLflow stores, or generated benchmark artifacts.
5. Include the experiment configuration and hardware metadata when proposing performance changes.

